// Objeto
const validator = {
  // Propiedades
  isValid : (cardnumber)=>{
    let reverse = "";
    // bucle
    for(let i = card.length-1; i >=0; i--){
      reverse = reverse + card[i]
      console.log(reverse)
    }
    
  },
  maskify : function(algo){
    console.log(algo);
  }
};

export default validator;
